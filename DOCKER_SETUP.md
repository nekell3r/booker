# Полный запуск через Docker

Все команды работают через Docker - **не требуется установка Go, protoc и других инструментов локально**.

## Быстрый старт

### 1. Полная настройка (всё в одном)
```bash
make setup
```

Эта команда:
- Запустит всю инфраструктуру (Postgres, Redis, Kafka, Prometheus, Grafana, Jaeger)
- Запустит все сервисы (admin-gateway, venue-svc, booking-svc, notify-svc)
- Выполнит миграции
- Заполнит тестовыми данными

### 2. Пошаговый запуск

```bash
# Запустить инфраструктуру и сервисы
make up

# Выполнить миграции (после запуска инфраструктуры)
make migrate

# Заполнить тестовыми данными
make seed
```

## Доступ к сервисам

После запуска доступны:
- **Admin Gateway**: http://localhost:8080
- **Grafana**: http://localhost:3000 (admin/admin)
- **Jaeger**: http://localhost:16686
- **Prometheus**: http://localhost:9090

## Основные команды

```bash
# Генерация proto файлов (через Docker)
make gen

# Запуск всех сервисов
make up

# Остановка всех сервисов
make down

# Просмотр логов
make logs

# Выполнение миграций
make migrate

# Заполнение тестовыми данными
make seed

# Пересборка всех образов
make build

# Пересборка и перезапуск
make rebuild

# Статус сервисов
make status

# Запуск тестов (через Docker)
make test
```

## Работа с отдельными сервисами

```bash
# Перезапустить конкретный сервис
make restart SERVICE=admin-gateway

# Выполнить команду в контейнере
make exec SERVICE=venue-svc CMD="sh"
```

## Остановка и очистка

```bash
# Остановить все сервисы
make down

# Остановить и удалить volumes (все данные будут удалены!)
docker-compose down -v
```

## Разработка

Все файлы монтируются в контейнеры, поэтому изменения в коде требуют пересборки:

```bash
# Пересобрать конкретный сервис
docker-compose build admin-gateway
docker-compose up -d admin-gateway

# Или пересобрать всё
make rebuild
```

## Требования

- **Docker** и **Docker Compose**
- Больше ничего не нужно! 🎉


